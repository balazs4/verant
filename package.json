{
  "name": "verant",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/balazs4/verant.git"
  },
  "author": "balazs4 <balazs4web@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prestart": "docker rm -f `docker ps -aq` || true",
    "start": "docker compose up",
    "dev": "npx onchange -i -k 'docker-compose.yml' -- npm run -s start"
  }
}
